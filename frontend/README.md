# important Read this before you push the code

As NextUI (old) updated the version 
So, follow the instructions below to get the latest version


migrate with npm
```javascript
npx @heroui/codemod@latest migrate
```

after that install the latest version

```javascript
npm install
```